[build]
  command = "npm install --prefix client && npm run build --prefix client"
  publish = "client/dist"

[build.environment]
  NODE_VERSION = "20"

# React Router (SPA): serve index.html for client-side routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
